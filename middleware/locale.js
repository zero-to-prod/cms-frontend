export default function ({ app, route, redirect }) {

  // if(app.$auth.user.user.locale !== app.i18n.locale){
  //   let routeName = app.getRouteBaseName(route.name);
  //   app.i18n.locale = app.$auth.user.user.locale
  //   redirect(app.localePath(Object.assign({}, route , {
  //     name: routeName
  //   })))
  // }

}
